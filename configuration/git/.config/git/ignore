# OSX files
.DS_Store

# vim-projectionist config
.projections.json

# Ctags
/tags

# Vim swap files
*.swp

# Postgres credentials files
.pgpass

# Git data directories
#
# These are automatically ignored by git but not by some other tools that
# respect this config file, like fzf and ripgrep.  Explicitly excluding them
# here won't change anything for git but will make those other tools act more
# like git.
/.git/

# Personal notes (files/dirs can appear anywhere, should always be ignored)
ne-notes*

# tmux initialisation files (used by custom scripts)
.tmux-init.sh
