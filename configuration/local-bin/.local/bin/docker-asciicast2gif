#!/usr/bin/env bash

DOCKER="${DOCKER:-docker}"

function DockerAsciiCast2Gif {
  $DOCKER run \
    --rm \
    --volume "$PWD:/data" \
    asciinema/asciicast2gif \
    "$@"
}

DockerAsciiCast2Gif "$@"
