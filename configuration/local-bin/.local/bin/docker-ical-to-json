#!/usr/bin/env bash

DOCKER="${DOCKER:-docker}"

function DockerIcalToJson {
  $DOCKER run \
    --rm \
    --volume "$PWD:/data" \
    --interactive \
    ical-to-json \
    "$@"
}

DockerIcalToJson "$@"
