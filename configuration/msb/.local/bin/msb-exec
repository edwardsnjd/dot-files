#! /usr/bin/env bash

# Execute a command in the first sandbox for this PWD

firstId="$(msb ps --label "msb.pwd=$PWD" --quiet | head -1)"

if [[ -n "$firstId" ]]; then
  exec msb exec "$firstId" -- "$@"
else
  echo "No sandbox found for this PWD" >&2
fi
