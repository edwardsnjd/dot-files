---
name: pdf-to-text
description: Extract the text from a PDF file.
---
Use `pdf-to-text < PATH` to extract the text from a PDF file.  Consider preserving the layout with `pdf-to-text -layout < PATH`.

Not all PDFs will have extractable text.  If that happens then give up and report the PDF unreadable.
