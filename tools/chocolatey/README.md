# Windows Tools

Scripts to install or update my daily driver tooling on Windows via `chocolatey`.

## Dependencies

- chocolatey

## Usage

*Chocolately requires elevated permissions so run the following scripts in a PowerShell window as admin.*

Install all tools initially:

```
install.ps1
```

To upgrade all tools that can be automatically upgraded:

```
upgrade.ps1
```
